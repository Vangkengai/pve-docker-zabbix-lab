version: '3.8'

services:
  nginx-proxy:
    image: nginx:alpine
    container_name: nginx-proxy
    restart: always
    user: root
    ports:
      - "8081:80"
      - "8082:8080"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - ./nginx/html:/usr/share/nginx/html:ro

  zabbix-agent:
    image: zabbix/zabbix-agent2:latest
    container_name: zabbix-agent2
    restart: always
    ports:
      - "10050:10050"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
      - ZBX_SERVER_HOST=192.168.31.81,172.17.0.1,127.0.0.1
      - ZBX_ACTIVESERVERS=192.168.31.81
      - ZBX_HOSTNAME=zabbix-agent2
